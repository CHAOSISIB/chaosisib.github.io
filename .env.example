# SMTP settings for Nodemailer
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=you@example.com
SMTP_PASS=yourpassword

# From and recipient
FROM_EMAIL=no-reply@chaos.isibangalore
TO_EMAIL=chaos.isibangalore@gmail.com

# Optional: allowed origin for CORS (set to your site URL)
ALLOWED_ORIGIN=https://your-site.example.com

# Rate limit
RATE_LIMIT_MAX=6
